- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "sixergame"."users"("id" bigserial NOT NULL, "name" text, "free_game_balance"
      float8 NOT NULL, "real_game_balance" float8 NOT NULL, "country_code" text NOT
      NULL, "phone_number" text NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: users
    schema: sixergame
  type: add_existing_table_or_view
