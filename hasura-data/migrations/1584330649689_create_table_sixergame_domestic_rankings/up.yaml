- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "sixergame"."domestic_rankings"("id" bigserial NOT NULL, "season_key"
      text NOT NULL, "team_key" text NOT NULL, "rank" text NOT NULL, PRIMARY KEY ("id")
      );
  type: run_sql
- args:
    name: domestic_rankings
    schema: sixergame
  type: add_existing_table_or_view
